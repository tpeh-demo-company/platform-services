apiVersion: fluxcd.controlplane.io/v1
kind: ResourceSetInputProvider
metadata:
  name: istio-platform-sandbox
  namespace: flux-system
  labels:
    app: istio
spec:
  type: Static
  defaultValues:
    version: "1.30.3"
    gatewayApiVersion: "v1.6.1"
    cnameTarget: "platform-sandbox.demo-company.site"
    kialiVersion: "2.30.0"